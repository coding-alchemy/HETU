# HETU 项目协作约束

本文件定义所有在本仓库工作的 AI Agent 必须遵守的项目级约定。

## 规划、设计与执行

### 1. 文档语言与质量

凡是需要用户审阅的需求文档、设计文档和执行计划，正文必须使用中文；代码、命令、路径、接口名、专有名词和必要的原文引用可保留原语言。

编写或修订设计文档时，必须使用 `karpathy-guidelines` 进行精简和自检：

- 明确假设、不确定项和必要取舍，不静默选择存在歧义的解释；
- 采用满足需求的最简单方案，不增加未要求的能力、抽象或扩展点；
- 每项设计内容必须直接对应实际需求，删除无关内容和重复表述；
- 给出能够直接验证核心结果的成功标准。

### 2. 文档归属与命名

本项目所有需求文档和设计文档统一放在项目根目录 `specs/`，使用 Markdown 格式，不得写入其他目录。

文档分别命名为：

- `YYYY-MM-DD-<topic>-requirements.md`
- `YYYY-MM-DD-<topic>-design.md`（或同日期前缀的 implementation 等后缀）

并遵循 `specs/` 下现有规格文档的统一格式：中文标题、文档版本、文档状态、创建/修订日期、适用范围等元数据，以及编号章节。

无法确定所属范围或命名时，必须先询问用户。除保留的 `plans/` 外，不得在 `specs/` 下创建其他目录层级。除非用户明确要求，不主动迁移或重命名既有文档。创建或修改规格后，检查其位于 `specs/` 且没有在其他目录留下副本。

### 3. 执行计划

执行计划放在 `specs/plans/<topic>/` 下，其中 `<topic>` 与需求或设计文档中的名称保持一致。子目录以 `README.md` 作为实施路线图，说明阶段目标、执行顺序、依赖关系和整体完成标准。

任务需要两个或以上具有先后依赖、可独立审阅或验收的阶段时，每个阶段使用独立的 `YYYY-MM-DD-NN-<step-title>.md`；单阶段任务同样按此命名，不使用 `plan.md` 之类的非编号文件名。

执行计划必须遵循 `specs/plans/README.md` 规定的目录、链接关系、任务粒度及测试门禁；创建或修改计划后，同步维护该索引。如果外部 Skill、插件或通用工作流给出的默认 spec/plan 路径与本文规则冲突，始终以本文件规定的项目路径和现有格式为准。

无法确定任务是否需要分阶段时，必须先询问用户。

### 4. 人工评审门禁

相关需求文档和设计文档获得用户明确批准后，才能编写执行计划；执行计划获得用户明确批准后，才能创建或修改实现代码。

用户未回复、仅查看文档、批准其他阶段或此前的一般性同意，均不视为当前阶段已批准。不得以任务简单、工期紧张或方案已经明确为由跳过人工评审。

### 5. Worktree 使用限制

未经用户显式批准，严禁使用 worktree。

## 核心目标守恒

设计、计划、实现、重构、迁移、验证及简单任务均适用。

### 1. 目标

先明确用户可观察的核心目标、直接证据及用户强调或纠正的硬约束；文件、计划、测试、提交仅为过程证据。

冲突优先级：用户最新核心目标、有效硬约束、核心工作、次要优化、形式偏好。目标、硬约束均须满足；硬约束仅经用户明确撤销或知情批准取舍后可变更。无法直接证明核心结果即未完成。

### 2. 变更

用户明确的指令、规则、模板、接口和既有行为均须执行，不得自由概括。组织调整不得改变范围、顺序、强度、粒度、行为或完成标准；相似替代、推迟、必选改可选等变更均须说明影响并获用户批准。

实施前完整读取权威基线，逐项映射“要求 → 阶段/验证 → 核心目标”，记录批准差异，审计、修复所有未批准的缺失或弱化；映射须具体到规则、步骤和行为。

### 3. 门禁

用户纠正方向时，停止旧框架，按最新要求重建目标、约束和映射并清理冲突或弱化；最新纠正优先于既有批准、设计和计划。

跨阶段或声明完成前须证明：

- 产物直达核心目标，要求与硬约束生效且未弱化；
- 核心结果可观察；验证覆盖行为而非静态结构；行为测试覆盖重组后的既有行为，在能力缺失时失败且输入不泄露答案；
- 静态映射、行为测试均成立且互不替代；
- 修改既有要求须完成逐项映射和负向差异审计，不适用须说明。

评审先问：“严格执行当前设计或计划，是否仍可能达不到核心目标？”证据缺失时，只能报告进展与缺口。

## Prompt 评审

默认 Prompt 面向主流大模型，是自然语言行为指令；除非用户明确要求，不按程序代码或形式化规范评审。

1. **先定义预期行为：** 明确目标模型、使用场景、核心目标和必须保留的行为；信息不足时说明假设，不静默猜测。
2. **评审行为而非字面：** 重点检查适用范围、条件、优先级、冲突处理、执行顺序和完成标准；改写或压缩时，对照原文确认这些行为没有改变。
3. **问题必须有实际影响：** 只有存在两种合理理解，并能用具体场景证明它们会导致不同决策时，才判定为实质问题；否则仅作为可选建议。
4. **证据与风险匹配：** 通常采用静态语义分析和最少量的行为试跑；只有用户明确要求或风险确实较高时，才增加形式化评测。
5. **坚持最小必要修改：** 只删除不影响模型决策的内容，只修复已确认的问题，不扩展范围、不重写无关部分，也不为假想场景增加规则。

评审通过的条件是：目标模型明确知道何时做什么，关键行为与原意一致，且不存在可能改变实际决策的合理歧义。

## 变更检查

- 提交前运行 `git diff --check`，并检查规格与计划中的相对链接有效。
