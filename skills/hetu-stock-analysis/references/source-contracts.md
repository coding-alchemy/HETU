# 来源合同

## 当前状态（放行前门禁）

本文件已写入五个数据域的首选/替代路径、逐域字段表、失败语义与等价边界，是 Agent 的按需决策资料，不是自动路由表。全部路径的接入状态仍为 `not_adopted`：**未接入，阶段 04 完成前不得标记替代取得**；阶段 04 已为四个结构化域提供最小来源适配器（机械助手，见下），其在线放行证据仍待阶段 04 验证批次落齐。放行证据为阶段 04 的离线合同回归、正常在线获取、受控禁用首选后的真实替代切换和不等价场景验证，以及用户对来源切换与不等价边界的确认。写入本文件不构成批准：没有字段依据的路径保持 `not_adopted`，不因出现在文档中而自动获批；阶段 04 在线证据若要求改变入口、字段依据或边界，须先按高风险变更处理。

## 共同失败分类与采用算法

来源适配器（接入后）只返回九种闭集状态之一和保存产物，不执行下一步：

`success`（取得并可解析）、`not_found`（检索面闭合但目标不存在）、`rate_limited`（来源限流）、
`transport_error`（连接、超时或维护页）、`permission_denied`（许可、授权或访问控制拒绝）、
`parse_error`（结构变化或无法解析）、`incomplete_pagination`（分页未闭合）、
`out_of_asof`（内容晚于带时区 `as_of`，过滤而非失败）、`scope_mismatch`（主体、期间、范围、单位或用途与当前工作包不符）。

阶段 04 起四个结构化域（公告及附件、财务报表、同业市场估值、交易状态与市场快照）提供最小来源适配器 `scripts/source_adapter.py`（机械助手，接口与边界见[工具目录](tool-catalog.md)）：输入为显式保存的原始响应与显式请求参数（`subject`、带时区 `as_of`、期间、范围、单位、用途、`disabled_sources`），输出固定信封 `{schema_version, adapter, domain, source_id, called, disabled, status, source_metadata, normalized, equivalence, raw_input_sha256}`。来源可用时 `called=true`、`disabled=false`、`status` 为上述九值闭集之一；来源被禁用（`disabled_sources` 含目标来源）时适配器在解析前短路，输出 `called=false`、`disabled=true`、`status=null`、`normalized=null`，不计入九种取数状态、不判为取数失败，是否改用替代由 Agent 决定。`equivalence` 只保留双方原值并做机械复算，不裁决采用；任一轴 `match=null`（保存响应未记录该轴或请求未提供）不构成该轴核验通过，不能单独支持「替代取得」标记，按未核验如实披露。来源元数据（身份、入口、采集时间、字段依据、许可标注五类要素，键名别名见工具目录）原样取自保存响应，适配器不生成、不补写；任一要素缺失、入口非 http(s) URL 或采集时间非法即拒绝该次适配调用（退出 1 失败信封）。`as_of` 判定使用全时刻精度（公告按毫秒时刻、快照按 `trade_date`＋可选 `trade_time`，时刻挂接来源时区——body 或 source_metadata 的 `timezone`，携带 `trade_time` 而未声明时区判 `parse_error`），同日 `as_of` 时刻之后的全部内容判 `out_of_asof`，不进入成功状态；同日无 `trade_time` 的快照时点不可证（`content_after_asof=null`），不能单独支持「替代取得」。主体与期间轴同样交叉核验 body 实际值（快照 `security` 按规范化代码、快照 `trade_date` 与财务报告期集合对照请求期间）：真实矛盾判 `scope_mismatch` 并保留全部原值，不可规范化的标识判 `match=null` 交 Agent 核验。行业需求与竞争域保持合同-only，不建适配器，适配器对该域正式拒绝。

采用算法固定为四步：

1. **记录首选分类和证据**：首选来源失败时，先落九值分类、保存的原始响应与失败定位，再考虑下一步。
2. **由 Agent 决定是否调用替代**：换源是 Agent 的明示决策，适配器和脚本不得后台自动换源。
3. **校验主体/期间/范围/单位/用途**：替代结果只有在主体、期间、范围、单位和用途都满足当前工作包时才可能等价。
4. **标记或保留**：满足等价校验的标记"替代取得"，不满足的保留原缺口或按 `存在冲突` 记录，不为完成率强行升级。

对 `cninfo-announcement-index`、`sina-financial-statements`、`tencent-quote-snapshot`，Agent 先选来源后才可选调用 `source_fetch.py`；脚本不自动换源，采集成功不代表采用。脚本只保存封闭来源的原始响应，`source_adapter.py` 仍按本合同重新判断闭合、主体、时点、范围、单位和用途，Agent 保持采用与替代决定权。

## 数据域总览（已批准矩阵）

| 数据域 | 首选 | 替代 | 必须保留的边界 |
| --- | --- | --- | --- |
| 公告及附件 | 巨潮索引与静态 PDF | 深交所披露页、发行人投资者关系页 | 证券、窗口、分页和时间未闭合时不能写“无” |
| 交易状态与市场快照 | 深交所公开状态或正式行情材料 | 巨潮身份/停复牌公告加腾讯、新浪或东财同日成交快照交叉验证 | 组合证据可证明正常成交，不冒充交易所精确状态字段 |
| 行业需求与竞争 | SEMI、SIA、正式政策统计或 authorized 数据库 | 同业年报、发行人年报及可追溯行业材料 | 方向与相对证据不证明精确公司份额 |
| 财务报表 | 发行人、巨潮或交易所正式报告 | 已保存新浪等结构化响应 | 替代值经 L0 核验，不覆盖调整口径 |
| 同业市场估值 | 同时点行情与正式股本 | 第二行情源加确定性复算 | 证券、时间、币种或股本口径不同即冲突 |

## 数据域字段合同

每个数据域使用同一字段表；"字段"行给出字段字典或一手字段依据，没有依据的路径保持 `not_adopted`。入口与字段依据来自 G1 审计批次（`.hetu/validation/phase-2-hardening/20260821T105141+0800/`）确认的实际可得路径。

### 公告及附件

| 字段 | 首选：巨潮索引与静态 PDF | 替代：深交所披露页、发行人投资者关系页 |
| --- | --- | --- |
| 身份 | 巨潮资讯网（监管指定信息披露渠道）；原始生产者为发行人与交易所 | 深交所披露页（交易所官方渠道）；发行人投资者关系页（发行人自营渠道，自述性） |
| 入口 | 公告索引查询 `www.cninfo.com.cn/new/hisAnnouncement/query`（HTTPS）；附件静态 PDF 仅拼接到 `https://static.cninfo.com.cn/` | 深交所披露页与发行人投资者关系页的公开 HTML/PDF 页面，按证券逐一核验定位 |
| 字段 | 索引字段 `announcementTitle`、`announcementTime`（毫秒整数）、`adjunctUrl`（相对路径）、`announcementTypeName`、`totalAnnouncement`、`hasMore`；真实响应不含 `pageNum`（20260822 批次实测键集：announcements/categoryList/classifiedAnnouncements/hasMore/totalAnnouncement/totalRecordNum/totalSecurities/totalpages）；条目解析由阶段 03 [announcement_index.py](tool-catalog.md) 承接，分页闭合由阶段 04 [source_adapter.py](tool-catalog.md) 按下行分页行规则判定；PDF 正文为非结构化文本，字段语义由 Agent 对照原文判断 | 页面为非结构化 HTML/PDF，无独立字段字典；标题、公告编号、日期由 Agent 逐项核对原文提取，不引入机器 schema |
| 证券映射 | 查询按六位证券代码加板块列定位；索引项以代码标识，不带交易所后缀 | 按发行人全称加证券代码双要素定位，防同名歧义 |
| 时间与时区 | `announcementTime` 毫秒时间戳按北京时间（`Asia/Shanghai`）换算后与带时区 `as_of` 比较；晚于 `as_of` 的公告属 `out_of_asof`，过滤而非失败 | 页面标注仅有披露日期、无官方时分秒时，按证据规则不得假定当日可得 |
| 更新频率 | 披露事件驱动，查询时点返回当前索引 | 事件驱动；发行人页更新节奏不保证与交易所同步 |
| 单位 | 条数、毫秒时间戳 | 同左 |
| 范围 | 单只证券的法定披露公告及附件 | 同一证券的法定与自愿披露；发行人页额外含自述性材料 |
| 分页 | 逐页采集并按请求页序保存（第 i 个保存页即请求页 i）；闭合信号为各页 `totalAnnouncement` 一致、跨页返回数与之相等、末页 `hasMore=false`（携带显式连续 `pageNum` 的合成 payload 按页码连续性闭合）；真实响应无 `pageNum`，`totalpages` 不可靠（实测 totalpages=2 而 totalAnnouncement=82，源自身矛盾），两者均不作为闭合信号；任一不闭合即 `incomplete_pagination`，不得据此写"无公告" | 页面级浏览无机器分页信号，覆盖完整性由 Agent 逐页确认后显式声明 |
| 许可 | 公开披露数据，`public`；遵守站点条款、robots 与访问频率 | 同左 |
| 保存限制 | 索引原始 JSON 与公开附件 PDF 按 `artifacts/raw/<source-id>/` 保存并登记 `manifest.json` | 公开页面与 PDF 同样保存；不保存登录、验证码或付费墙后内容 |
| 失败语义 | 分页未按上行闭合信号闭合（`totalpages` 与总数矛盾时不采信该信号）→`incomplete_pagination`；维护页或连接失败→`transport_error`；检索面闭合且无结果→`not_found`；结构变化→`parse_error` | 页面改版→`parse_error`；访问被拒→`permission_denied` |
| 适配器 | 采集前由 Agent 明示选择 `cninfo-announcement-index`；`source_fetch.py` 负责 HTTPS orgId 匹配与完整分页保存，随后经 [source_adapter.py](tool-catalog.md) 重新判断闭合（body 为巨潮分页 `pages`，条目解析由阶段 03 `announcement_index.py` 承接，分页闭合由适配器按 `totalAnnouncement` 一致＋返回数相等＋末页 `hasMore`＋请求页序＝保存页序自行判定——`totalpages` 不可靠不作信号，携带显式连续 `pageNum` 的合成 payload 按页码连续性闭合）：分页不闭合或保存页列表为空→`incomplete_pagination`、检索面闭合且空→`not_found`、全部公告晚于 `as_of`（按毫秒时刻，含同日 `as_of` 时刻之后）→`out_of_asof`；禁用时经 `disabled_sources` 在解析前短路（`called=false`、`status=null`，见共同失败分类节） | 无独立适配器；替代页由 Agent 按原文核验，不引入机器 schema |
| 适用边界 | 证券、窗口、分页和时间未闭合时不能写"无" | 只作首选失败后的定位与交叉核验；报告事实仍回链正式披露原文 |

### 交易状态与市场快照

| 字段 | 首选：深交所公开状态或正式行情材料 | 替代：巨潮身份/停复牌公告加腾讯、新浪或东财同日成交快照交叉验证 |
| --- | --- | --- |
| 身份 | 深交所（交易所官方，交易状态的原始生产者） | 巨潮公告（身份与停复牌的 L0 定位）＋腾讯、新浪或东财行情快照（传播渠道，须交叉验证） |
| 入口 | 深交所公开端点无个股交易状态字段（20260822 批次实测：市场总览页为 JS 壳、ShowReport 为市场层证券类别统计，无单只证券状态字段；G1 中该接口曾返回维护页），交易所精确状态字段保持缺口；可达首选路径为正式行情材料与组合证据——巨潮身份/停复牌检索＋同日成交快照交叉验证（入口同替代列），组合证据不冒充交易所精确状态字段 | 巨潮公告索引（同公告及附件域入口）；腾讯收盘快照 `https://qt.gtimg.cn/q=` 加 `sz` 或 `sh` 前缀与六位代码；新浪、东财公开同日快照 |
| 字段 | 交易所证券状态字段（停牌/复牌/风险警示等官方口径） | 公告侧：停复牌公告标题与正文事实；快照侧：同日收盘价、涨跌幅、成交量、总市值等来源标签字段；腾讯总/流通市值标签有互换先例，须保留原值不自动交换 |
| 证券映射 | 交易所证券代码 | 快照按 `sz`/`sh` 前缀加六位代码；多源须映射到同一证券并逐一核对 |
| 时间与时区 | 交易状态按交易日与北京时间 | 快照时间戳按北京时间换算；"同日"按 `as_of` 交易日核对，跨日快照不得混用 |
| 更新频率 | 随交易所公告更新 | 快照为收盘后或盘中时点值，逐次采集 |
| 单位 | 状态枚举 | 价格为元、成交量为股/手、市值按来源标签单位（如亿元）逐项记录 |
| 范围 | 单只证券的交易状态 | 单只证券的身份与状态组合证据 |
| 分页 | 无分页 | 无分页 |
| 许可 | `public`；遵守交易所站点条款 | `public`；腾讯、新浪、东财公开接口按站点条款与访问频率使用 |
| 保存限制 | 原始状态响应或页面保存 | 各源同日快照原始响应逐源单独保存，不合并改写 |
| 失败语义 | 接口维护或不可得→`transport_error`；官方字段无可得路径→`not_found` | 快照结构变化→`parse_error`；限流→`rate_limited`；授权不符→`permission_denied` |
| 适配器 | 当 Agent 明示选择 `tencent-quote-snapshot` 时，`source_fetch.py` 保存单证券原始文本和机械字段；经 [source_adapter.py](tool-catalog.md) 处理主体、时点和标签复核（快照 body 必含 `trade_date`、`price`；交易快照晚于 `as_of`→`out_of_asof`） | 同左：经 `source_adapter.py` 解析；标签市值与价格×股本复算双值保留、不交换标签；禁用时经 `disabled_sources` 在解析前短路（`called=false`、`status=null`） |
| 适用边界 | 官方单一状态字段难以稳定取得时，按[恢复规则](recovery.md)降级并收缩表述 | 组合证据可证明正常成交，不冒充交易所精确状态字段；停牌、风险警示结论只能收缩表述 |

### 行业需求与竞争

| 字段 | 首选：SEMI、SIA、正式政策统计或 authorized 数据库 | 替代：同业年报、发行人年报及可追溯行业材料 |
| --- | --- | --- |
| 身份 | SEMI、SIA（行业协会，独立于发行人的原始统计生产者）；正式政策统计（政府或监管发布）；authorized 数据库（用户明确授权的第三方） | 同业年报与发行人年报（发行人自述）；可追溯行业材料（须能定位原始出处） |
| 入口 | `semi.org`、`semiconductors.org` 新闻稿与统计页；正式政策统计发布页；authorized 数据库按授权入口访问 | 巨潮或交易所年报 PDF（同公告及附件域入口）及同业公告 |
| 字段 | 行业销售额、预测增速等具名统计指标（自然语言或表格，无机器字段字典）；指标名称、归属期间与单位须逐项从原文核对 | 年报中的行业叙述、收入结构、经营数据；Agent 对照原文逐项核验，不引入机器 schema |
| 证券映射 | 不适用（行业层口径） | 同业证券代码集合逐一映射 |
| 时间与时区 | 统计归属期间与发布时间分开记录；协会发布时区按来源标注（如 `-04:00`、`-07:00`）换算后核对时点 | 年报归属期间与公告日按北京时间记录 |
| 更新频率 | 协会按季度或年度发布 | 定期报告披露节奏 |
| 单位 | 原币种单位（如亿美元），不做未声明的换算 | 原文单位 |
| 范围 | 全球或区域行业口径，与中国市场口径差异必须显式记录 | 公司叙述与行业背景 |
| 分页 | 无分页（文档级） | 无分页 |
| 许可 | 公开新闻稿与统计页 `public`，完整数据集受限时不绕过；authorized 数据库仅限授权范围 | `public` 年报 |
| 保存限制 | 公开新闻稿与统计页保存；受限部分只记录许可允许的定位与摘要 | 年报与公告 PDF 保存 |
| 失败语义 | 受限或授权过期→`permission_denied`；不可得→`not_found` | 原文无法核验→`parse_error`；口径不符→`scope_mismatch` |
| 适配器 | 不建（合同-only；`source_adapter.py` 对该域正式拒绝） | 同左 |
| 适用边界 | 协会全球口径不等于中国设备资本开支，更不等于公司份额 | 方向与相对证据不证明精确公司份额；发行人自述市占率不可升级为独立验证；媒体转引只作背景 |

### 财务报表

| 字段 | 首选：发行人、巨潮或交易所正式报告 | 替代：已保存新浪等结构化响应 |
| --- | --- | --- |
| 身份 | 发行人定期报告（L0 原始生产者）；巨潮、交易所为传播渠道 | 新浪财经结构化报表（L1 可追溯数据渠道） |
| 入口 | 巨潮公告索引定位年报/季报 PDF（同公告及附件域入口） | 结构化财报接口 `quotes.sina.cn/cn/api/openapi.php/CompanyFinanceService.getFinanceReport2022?paperCode=<sz|sh 前缀＋六位代码>&source=<lrb|zcfzb|xjllb>&type=0&page=<页码>&num=<期数>`（返回 `result.data.report_list` JSON，保存后离线处理；20260822 批次实测）；新浪 vFD 三表页 `money.finance.sina.com.cn/corp/go.php/vFD_ProfitStatement/` 等实测为服务端渲染 HTML，仅作人工浏览入口，不作机器结构化入口 |
| 字段 | PDF 报表科目与附注，Agent 逐项对照原文提取 | 保存响应的 `report_list`（按 `YYYYMMDD` 报告期）与 `item_title`/`item_value`/`item_tongbi`；真实响应 `item_value` 可为显式 `null`（银行/保险模板等不适用科目，适配器构建 normalized 视图时仅跳过显式 `null` 行；缺失 `item_value` 键的行不在容差内，按结构偏差判 `parse_error`）且 `item_tongbi` 可为数值型（适配器按原值转字符串），该容差仅由阶段 04 适配器施加，阶段 03 [financial_statements.py](tool-catalog.md) 保持仅字符串 Schema、东财等 list 形 payload 正式拒绝。东财 push2 f 码口径由阶段 04 [source_adapter.py](tool-catalog.md) 按自带字段字典（`f183` 营业总收入、`f184` 营业总收入同比增长、`f185` 归母净利润同比增长、`f186` 销售毛利率、`f188` 资产负债率；一手依据见工具目录"独立字段字典一手依据"节）解析，不进入阶段 03 财务标准化器 |
| 证券映射 | 公告及附件域的证券映射 | `stockid` 六位代码 |
| 时间与时区 | 报告期与公告日按北京时间记录 | `report_list` 键为报告期日期；采集时间与 `as_of` 按北京时间核对 |
| 更新频率 | 定期报告披露后更新 | 随披露更新，具体延迟未承诺 |
| 单位 | 报表原币种（人民币元）及原表单位 | `item_value` 原值字符串，单位语义以 L0 报表为准 |
| 范围 | 合并或单体口径按报告原文显式记录 | 结构化行默认口径须核对；调整前后列不区分时由 L0 裁决 |
| 分页 | 无分页（文档级） | 无分页 |
| 许可 | `public` | `public`；遵守站点条款与访问频率 |
| 保存限制 | 报告 PDF 保存 | 结构化原始响应先保存、后解析；原始与解析产物分别登记 |
| 失败语义 | 文档缺失→`not_found`；不可读的已保存报告→`parse_error` | 结构变化→`parse_error`；报告期或合并范围不符→`scope_mismatch`；晚于 `as_of` 的报告期→`out_of_asof` |
| 适配器 | 无独立适配器（正式报告 PDF 由 Agent 逐项对照原文提取） | 当 Agent 明示选择 `sina-financial-statements` 时，`source_fetch.py` 保存 `sina_report_list`；经 [source_adapter.py](tool-catalog.md) 施加现有容差后解析（`sina_report_list` 经适配器按字段行已证容差预处理后交阶段 03 `financial_statements.py` 承接；东财 push2 口径按适配器自带字段字典解析）：报告期或合并范围不符→`scope_mismatch`、全部报告期晚于 `as_of`→`out_of_asof`、空 `report_list` 或每期全部科目值为空→`not_found`；禁用时经 `disabled_sources` 在解析前短路（`called=false`、`status=null`） |
| 适用边界 | 追溯调整、重述以截至 `as_of` 最新正式披露的调整后可比列为准 | 替代值经 L0 核验，不覆盖调整口径；不与调整后可比列混拼连续序列 |

### 同业市场估值

| 字段 | 首选：同时点行情与正式股本 | 替代：第二行情源加确定性复算 |
| --- | --- | --- |
| 身份 | 首选行情源（如腾讯收盘快照，L1 传播渠道）＋正式股本披露（发行人公告或交易所，L0） | 第二行情源（新浪、东财等公开快照） |
| 入口 | 腾讯收盘快照 `https://qt.gtimg.cn/q=` 加 `sz` 或 `sh` 前缀与六位代码（同业批量）；股本取发行人年报或股本变动公告 | 第二行情源的同日快照接口，按源分别调用 |
| 字段 | 收盘价、总股本、流通股本及来源标签字段 | 同类字段；总/流通市值标签有互换先例，双方原值保留，不自动交换 |
| 证券映射 | 同业代码集合（`sz`/`sh` 前缀加六位代码）逐一映射并与研究主体核对 | 同左；两源证券名单须逐一对齐 |
| 时间与时区 | 快照时间戳按北京时间，与 `as_of` 同一交易日 | 同左；两源采集时点分别记录 |
| 更新频率 | 收盘后可得 | 收盘后可得 |
| 单位 | 价格为元、股本为股、市值按来源标签单位 | 同左；单位标签逐项记录，不跨源换算 |
| 范围 | 同业集合的同一时点估值 | 同左 |
| 分页 | 无分页 | 无分页 |
| 许可 | `public` | `public` |
| 保存限制 | 快照原始响应与股本依据分别保存；两源产物不互相覆盖 | 第二来源原始响应单独保存，不覆盖首选 |
| 失败语义 | 接口失败→`transport_error`；限流→`rate_limited` | 标签与价格乘股本复算不一致时不自动交换标签，按 `存在冲突` 保留双方原值；复算用阶段 03 [numeric_consistency.py](tool-catalog.md) 的 `market_cap_values` 与 `compare_metric` |
| 适配器 | 同业逐证券处理时，Agent 可明示选择 `tencent-quote-snapshot` 并由 `source_fetch.py` 保存单证券原始文本和机械字段；经 [source_adapter.py](tool-catalog.md) 解析（快照 body 必含 `trade_date`、`price`；主体、期间、范围、单位或用途不符→`scope_mismatch` 双值保留；快照晚于 `as_of`→`out_of_asof`） | 同左：经 `source_adapter.py` 解析，第二来源响应单独保存、单独适配，两源产物不互相覆盖；禁用时经 `disabled_sources` 在解析前短路（`called=false`、`status=null`） |
| 适用边界 | 股本口径须来自正式披露并记录基准日 | 证券、时间、币种或股本口径不同即冲突，标记 `存在冲突` 并保留双方原值；快照 body 实际 `price_unit` 与申报单位按规范币种族比较（元/CNY 同族等），真实矛盾判 `scope_mismatch`、描述性标签判 `match=null` 交 Agent 核验（requested/payload/body 三值原样保留） |

## 合同范围外的披露缺口

以下缺口不因本合同存在而被替代或掩盖：**未披露订单**（在手合同、订单金额无公开口径）保持披露缺口，合同负债、备产存货等间接指标不得表述为订单验证；单日涨跌归因与全天资金流收盘终值不建设正式来源合同或适配器；精确公司份额在无合法等价来源时必须保持 `未取得`，不以行业方向或同业披露冒充。

## 切换与错误语义（放行后生效）

来源适配器只返回九值闭集状态和保存产物。首选失败后，Agent 先记录分类，再由 Agent 决定是否调用替代；适配器不得后台自动换源。替代结果只有在主体、期间、范围、单位和用途都满足当前工作包时才是"替代取得"；不等价结果保留原缺口，不为完成率强行升级。接入阶段 04 前，来源失败按[恢复规则](recovery.md)与证据规则处理；**任何结果——包括宿主原生合法替代取得的——都不得标记"替代取得"**，只能按正式状态语义记录为尝试、边界或`未取得`/`存在冲突`并如实披露差异；"替代取得"语义自阶段 04 放行证据齐备并经用户确认后才生效。
