# 二期加固分析工具候选归档

> 归档状态：非生产、未接入 canonical Skill、待二期加固评审与整合
> 实际分析时间：2026-08-16T18:54:25+08:00
> 适用范围：GLM 独立研究运行中产生的一次性采集脚本及其可复用原子能力
> 二期加固需求：[`../../specs/2026-08-17-stock-analysis-workflow-v1-phase-2-hardening-requirements.md`](../../specs/2026-08-17-stock-analysis-workflow-v1-phase-2-hardening-requirements.md)

## 1. 边界结论

本目录只保存取证副本、三个离线候选和对应回归测试。候选不进行网络访问，不选择数据源，不判断
研究覆盖，不裁决冲突，也不生成报告结论。它们尚未进入
`skills/hetu-stock-analysis/**` 或 Skill manifest，不能按生产能力使用。

原始脚本不能直接修补后投入生产：它把采集、字段猜测、覆盖重试、文件写入和业务含义混在一个
流程中，既不能稳定复现，也无法证明 `as_of`、字段语义和同名产物口径始终正确。

## 2. 取证来源与完整性

- 原始来源：`/private/var/folders/t5/v8qdfrqd0t71sgbm2bl0r5b00000gn/T/opencode/naura-independent/fetch_all.py`
- 归档副本：[`original/fetch_all.py.txt`](original/fetch_all.py.txt)
- SHA-256：`dee6531044fb631e8ce154fe3ef74dd88871aa3535fa69092043fd1b8063820d`
- 完整性结果：复制前后 SHA-256 一致；副本使用 `.txt` 后缀且没有文件执行位。
- 关联研究：`.hetu/research/002371.SZ-20260816T175228+0800-naura-independent/`

临时目录只用于说明来源，不能作为后续依赖。归档副本保留错误现场，不得导入、安装、执行或作为
候选实现复制回 Skill。

## 3. 已确认问题与修复要求

| 编号 | 已确认问题 | 实际影响 | 二期修复要求 |
| --- | --- | --- | --- |
| P-01 | `fetch_all.py` 把东财 `f183` 写为营收同比、`f186` 写为净利同比、`f188` 写为毛利率 | 样本中三值分别等于营业收入 `10,322,863,908.82`、由收入与成本复算的毛利率 `40.7727895%`、由负债与资产复算的资产负债率 `50.0095332%`，与脚本标签不一致，会直接制造错误事实 | 禁止仅凭序号命名字段；保存字段映射依据，并用独立报表复算校验百分比语义；冲突时失败关闭 |
| P-02 | `analysis/.../collect.py` 将腾讯报价索引 44/45 标为总市值/流通市值，和股价乘总股本/流通股本的复算顺序相反 | 总市值与流通市值标签互换 | 用同一时点的股价和两种股本分别复算；输出两个显式命名值，不自动交换来源标签 |
| P-03 | `fix3.py` 用新浪资金流的不同字段结构覆盖已有同名 `fund_flow_120d.csv` | 同一路径的列语义随补采脚本变化，后续读取者无法知道口径 | 来源、Schema 或口径不同必须使用不同产物名和元数据；禁止静默覆盖 |
| P-04 | 证券代码、截止日期和绝对输出路径散落硬编码 | 脚本不能安全复用于其他证券、时点或宿主 | 输入必须显式传入并校验；候选函数不持有证券、时点或文件系统状态 |
| P-05 | 多数采集没有把 `as_of` 实际应用到请求或结果过滤 | 可能混入基准日后信息 | 每条时点敏感记录保留原始时间；使用显式时区过滤，并测试边界时刻 |
| P-06 | 原始响应、字段映射依据、HTTP 状态和响应结构保存或校验不足 | 无法重放字段语义，异常页也可能被当成有效数据 | 保存原始响应和来源元数据；解析器对缺字段、错类型和异常结构失败关闭 |
| P-07 | 巨潮证券表使用 HTTP，公告查询只处理单页 | 传输和公告覆盖都不可靠 | 只允许 HTTPS；记录总数与已取数量，分页不完整必须显式暴露，不能写成完整检索 |
| P-08 | 最终资金流依赖主脚本之外的多次补采脚本 | 单次运行无法完整复现最终产物 | 正式整合前建立确定的采集合同、产物命名和失败状态；一次性修复脚本不得进入 Skill |

## 4. 保留的候选代码

1. [`candidates/financial_statements.py`](candidates/financial_statements.py)：只标准化已取得的新浪财务
   报表结构，按报告期排序并原样保留数值字符串。
2. [`candidates/announcement_index.py`](candidates/announcement_index.py)：只标准化已取得的巨潮公告
   索引，强制时区化 `as_of`、HTTPS 附件地址和分页完整性信号。
3. [`candidates/numeric_consistency.py`](candidates/numeric_consistency.py)：只用 `Decimal` 复算毛利率、
   资产负债率、市值和市盈率，并返回一致或冲突，不自动纠正来源值。

这些候选只使用 Python 标准库，无网络调用、绝对路径或固定证券/日期。测试位于 [`tests/`](tests/)，
TDD 记录见 [`tests/red-tests.txt`](tests/red-tests.txt) 和
[`tests/green-tests.txt`](tests/green-tests.txt)。

## 5. 删除与保留清单

本次完成验证后删除以下无复用价值、未跟踪的一次性代码：

- `analysis/naura-002371-2026-08-16/collect.py`
- `analysis/naura-002371-2026-08-16/fix_failed.py`
- `analysis/naura-002371-2026-08-16/fix2.py`
- `analysis/naura-002371-2026-08-16/fix3.py`

保留该目录内的 `request.json`、`data/` 和 `runs/`（如存在）作为研究审计产物；不把这些研究数据
纳入本次代码提交。原始错误 `fetch_all.py` 已由哈希锁定的 `.txt` 副本单独保存。

## 6. 二期整合门禁

候选只有同时满足以下条件，才可另行设计并最小化接入 canonical Skill：

1. 对拟支持的每个来源保存字段字典或一手字段依据，不能把单个样本的算术吻合当成完整字段合同。
2. 补齐异常结构、零分母、单位、时区边界、分页不完整、`as_of` 前后和已知标签互换回归场景。
3. 对原始响应、标准化产物和来源元数据定义可重放合同；不同来源或 Schema 不得覆盖同名产物。
4. 候选保持原子和确定性；Python 不决定工作包覆盖、来源优先级、事实裁决或报告结论。
5. 先做独立设计和 RED→GREEN 评审，再更新 Skill 资源及 manifest；取证副本绝不直接安装。
6. 接入后通过现有 Skill 全门禁、冻结区差异审查、真实多模型报告回归和用户人工确认。

未通过任一门禁时，候选继续留在本归档，不得以“部分可用”名义进入生产路径。
