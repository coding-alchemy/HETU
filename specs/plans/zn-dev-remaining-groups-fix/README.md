# zn_dev 剩余四项集中修复路线图

> 文档版本：v1.2
> 文档状态：阶段 01–05 已执行完成。阶段 04 复评七项缺口与第二轮 2 项经两轮复评收口
> （合并提交 `20fb6ff`）；2026-09-25 复评后修复 host-tools.md 命令名阻断项；阶段 05
> 全面评审 4 项迁移前阻断问题经用户批准后修复完成（`8e0177f`）；未迁移、未推送
> 创建／修订日期：2026-09-25（v1.2：登记阶段 05 批准与完成）
> 设计依据：[修复设计 v1.1](../../2026-09-24-zn-dev-remaining-groups-fix-design.md)
> 上级：[计划索引](../README.md)
> 执行者：任意模型和 Harness，单 Agent 顺序执行；有 `superpowers:executing-plans` 时按阶段执行。
> 目标：修复已证缺陷、保留已验收成果，重新判定第 5–8 组迁移条件；不新增需求组。
> 技术路线：现有 Python CLI、SQLite 只读导出和 pytest；不建框架，不新增宿主或模型调用。

## 1. 范围与授权

设计订正与编制计划已获用户同意，本计划尚待批准。批准本计划并明确执行后，可在 zn_dev
实施列明修复、定向验证、精确暂存并作本地提交；不授权推送、强推、合并或下一批迁移。
不使用 worktree，不修改 main、AGENTS.md、真实安装、宿主配置或 `.hetu/` 原始证据。

起点记录为 `zn_dev@a044bb2`、main `751ed53`。执行前核实实际状态；仅身份变化不触发
重验。保留上一轮 execution-implementation.md 的未提交状态订正，不覆盖或混入修复提交。
新增修复设计及本目录属于本任务；不得 `git add .`。有无关变更则只隔离本次文件范围，
同文件无法安全分开时说明冲突，不 stash/reset/清除用户内容。

纳入：5-A1/A2/B1；6-A1～A4、B1/B3（B2 为随修负例）；8-B1。
不纳入：5-B2/B3、6-C 额外加固、7-B1/B2 补记。6-B4 重入缺陷撤回，workflow 不修改。
第 7、8 组的认证、采样和校准暂缓；M2-6、中期 B1–B6 不重开；Linux/ext4 不重新列任务。

## 2. 阶段与检查点

| 阶段 | 可审阅产物 | 依赖与停止门 |
|---|---|---|
| [01 计量关联与完整性](2026-09-24-01-metering-integrity.md) | 已证身份关联、结算策略、导出与重试回归 | 已完成（ebfbd8d），原生语义证据充分、无能力缺口 |
| [02 扩展与辅助测试](2026-09-24-02-extension-and-portability.md) | 版本/登记表修复、解析器与历史审计边界 | 已完成（dc2ca33） |
| [03 最终验证与状态重判](2026-09-24-03-validation-and-handoff.md) | 一次最终门禁、干净副本检查、四项状态表 | 已完成：门禁 1592 passed/6 skipped（均为带理由宿主可用性 skip），四项状态见阶段文末 |
| [04 复评七项缺口集中修复](2026-09-24-04-review-fix-roundup.md) | 复评 7 项行为合同缺口的修复与直接回归 | 已完成（两轮复评收口，合并提交 `20fb6ff`）；2026-09-25 复评后修复 host-tools.md 命令名阻断项 |
| [05 全面评审 4 项迁移前阻断问题修复](2026-09-25-05-review-blockers-fix.md) | 卸载边界、check 计时 fail-open、夹具覆盖保护、描述续行解析的修复与定向回归 | 已完成（2026-09-25 批准后执行，实现提交 `8e0177f`）；4 项阻断关闭，`--permission-prompt-tool` 支持性登记为未确定 |

一次批准执行全部阶段时可按顺序推进，不每个小步骤重新询问；用户仅批准某阶段时停在
该阶段。需求取舍、扩大接口或必需能力无法取得时，保留现场并报告，不自行弱化要求。
每阶段回退仅撤销本批可定位改动；已有提交用后续纠正，不整树恢复、不删备份。

## 3. 验证和有效性

先对新行为写负例并确认旧实现失败，再修复；只变更本批问题，不夹带重构。
原生证据只读，生产数据库不是 CI 测试依赖；回归在 tmp_path 合成数据库/登记表上执行。
第 01/02 定向回归各在改动后运行；第 03 一次全量工程门禁，不再重复两态全量。
`.hetu/` 缺失和损坏负例只在临时源码副本中验证，不搬动、重命名或改写真实目录。

只有实际语义变化直接影响的导出/扩展/解析路径重验；既有历史研究与已关闭账目不重算。
测试数与 skip 数据实分类，不能照抄基线 1544/5 或用条件跳过隐藏活功能回归失败。
本批真实股票分析、宿主会话、模型补验次数均为 0；工程测试不会启动此类运行。

## 4. 完成标准

每个纳入问题都给出：已证原因、修复位置、失败后通过的行为证据及剩余边界。
未知用量不为零、数量对齐不替代正确归属、登记表错误不先写后报错、历史审计跳过不代表
性能通过。四组分别报告实现、有效证据、未关闭问题及迁移条件，不把修复通过说成认证通过。
最终停止交用户复评；本地提交不等于远端已同步，不自动推进迁移。
