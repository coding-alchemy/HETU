# 五期阶段 04：正式并行与全流程优化

> 文档版本：v1.2
> 文档状态：2026-09-12 用户批准开发级收尾；合同实现与决策验证完成，实际派发、重派和失败隔离执行缺项移至 07；不含股票分析授权
> 批准日期：2026-09-08
> 创建／修订日期：2026-09-08／2026-09-12
> 适用范围：单股内部按问题拆分、隔离、汇合与有证据的性能改动
> 上级：[路线图](README.md)；前序：[03](2026-09-08-03-control-context-reuse.md)
> 设计依据：[§2.3、§2.5.4–7、§7](../../2026-09-06-stock-analysis-workflow-v1-phase-5-design.md)
> 执行方式：使用 `superpowers:executing-plans`；研究并行由产品主 Agent 决定。

## 1. 范围和前置条件

03 合同实现与现有受控验证已获用户接受为开发级收尾；原生取消、恢复及 R03 故障执行验证移至 07，不再作为本阶段开发前置。保留本阶段必要的局部质量与安全验证，未验证能力不得声明正式支持；02 数值待定（按 2026-09-11 performance-approval 批准：不阻塞实现与行为验证，约束数值发布与最终性能判定）；01 可观测真实任务区间。
本阶段完成并行机制及最小行为验证，07 用获批完整研究证明收益，不以代码具备接口宣称提速。

| 要求 | 任务 | 直接验证 |
|---|---|---|
| 长期 2.4.1–2 | 04.1 | 随实际问题／资料变化拆分，复用后不重复派发；依赖和默认串行保持 |
| 长期 2.4.3–5 | 04.1 | 单域自足输入、最小权限、完整返回、共享文件无并发写、无越权委派 |
| 长期 2.5.1–4 | 04.2 | 同源去重、口径统一、按来源裁决、局部失败及授权再审 |
| 长期 2.6.1–3、2.7、五期 3.2–3.3 | 04.2、04.3 | 尝试采用可追踪、至少三域重叠、同质量顺序对照及全流程目标 |

## 2. 文件与接口

修改 `skills/hetu-stock-analysis/references/orchestration.md`、`host-tools.md`、`recovery.md`、
`checkpoint.md`、`work-package-result.md` 及实际受影响 core owner 文件；保持 catalog 的
W0–W10 和官方 ID。必要时同步 `artifact-contract.md` 和 `scripts/check-run-artifacts.py` 的
显式引用检查，不增加研究调度代码。新增 `tests/product/fixtures/phase5_parallel/` 的受控
研究材料／独立评审期望及 `tests/product/skill/test_phase5_parallel_contract.py`。

任务与尝试、父子和采用记录复用 03 的合同。正式子任务不是新核心包，不新增任务目录层级：
产物放在 `artifacts/raw/<source-id>/`、`normalized/<owner>/`、`derived/<owner>/`、
`scripts/<owner>/`，以任务及尝试标识区分文件。主 Agent 分配可写文件，公共输入只读；子任务
不得写 manifest、checkpoint、evidence、owner 正文和最终报告，返回后由 owner／主 Agent 串行合入。

## 3. 任务 04.1：明确派发条件与自足任务合同

- [x] 在现有临时子问题规则之外说明正式子任务的标识、独立生命周期、尝试与采用记录；不改变
  核心依赖或把临时问题注册进 catalog。两个以上无共享可变状态的独立问题才评估并行。
- [x] 固定派发前统一项：证券／发行人、`as_of`、模式、深度、关注点、授权与证据规则。候选域
  按问题选择；充分旧资料按复用规则使用，不凑固定域数，不省略剩余必需研究。
- [x] 请求规范化、依赖财务基线的预测估值、跨域综合、最大未知、监控、质量复核和报告默认串行；
  数据依赖、共享状态、受限资源和高冲突场景先满足依赖，不能靠子任务数量强行并行。
- [x] 给每个子任务自足输入：目标、主体、时点、模式、授权、深度、来源期望、禁止项、完成
  条件、只读输入、可写文件及返回格式。上下文只传所需部分，原生授权不扩大、不无界嵌套。
- [x] 返回必须包含任务标识、查询摘要、来源定位、事件／发布时间、事实、计算输入、判断、
  反证、冲突、缺口、不确定性、后续验证、产物与失败登记。没有必需信息如实返回不足。
- [x] 真实短样本 P01 提供三项独立问题和一项依赖财务结果的问题；P02 将其中一项资料预先
  合法补齐。观察 P02 不再重复派发该问题，依赖问题等待基线，公共权威文件无并发写。
  （04.1 完成，2026-09-12：合同文本提交 d7f49b0——orchestration"正式子任务与并行派发"节
  （门槛/统一项/默认串行/自足输入十二项/返回契约十三项）、host-tools 最小权限句、
  work-package-result 串行合入句、静态测试 10 项＋任务评审通过；P01/P02 行为样本通过，
  见 .hetu/validation/phase-5/20260912-stage04-parallel/p-samples-summary.md）
## 4. 任务 04.2：汇合裁决、失败隔离与采用

- [x] 主 Agent 按完成度、来源、时点、授权、证据类型和结论边界逐项审查，去重同源转载、统一
  单位、回源裁决冲突。多数、速度或子 Agent 自报置信度都不作采用依据。
- [x] 失败仅影响对应问题，在预算内重试、缩小、重新分派、顺序执行或局部降级；影响综合的
  问题达到可判断状态且原依赖满足才综合。无子 Agent 时同质量顺序完成，标未完成并行验证。
- [x] authorized 结果合入前重新核对实际来源、数据集、许可、点时、必要性与脱敏，不把
  “子任务完成”当授权证明。取消后的迟到结果按 03 规则未恢复不采用。
- [x] 先写 fixture：两个同源转载对一个独立原文、万元／亿元冲突、晚于时点的材料、越权返回、
  一个超时任务和一个正常任务。独立评审答案不在研究侧，加入正常材料对照。
- [x] 真实 Agent 试跑 P03 验证可信冲突证据胜过更快／更多返回，P04 验证失败重派只影响本域，
  P05 验证无子 Agent 的顺序回退，P06 验证越权及恶意子任务被拒绝且其他研究继续。
- [x] 核对 checkpoint、owner 和 manifest 的父子、尝试、旧新差异和唯一采用；禁止从最终
  报告事后补造关系，保留失败脚本和不完整产物。
  （04.2 完成，2026-09-12：合同文本提交 836af1f——orchestration"汇合裁决与失败隔离"节
  （六维裁决/同源去重/单位统一/回源裁决/否定句/五处置/综合门控/顺序回退标注/authorized
  六项再审/记录核对）、recovery 越权处置句块、phase5_parallel 夹具 12 件、静态测试 15 项
  ＋任务评审通过；P03–P06 行为样本由协调层运行全部通过，见
  .hetu/validation/phase-5/20260912-stage04-parallel/p-samples-summary.md）
## 5. 任务 04.3：落实有证据的优化与比较准备

- [x] 从 02 的 impact-map 逐项实施最小改动；03 已减少的规则／长材料重复读取不重复改写。
  只处理证实的重复能力发现、同问题检索／解析、脚本重写、机械记录修正和低收益重试。
- [x] 必需工具失效时仍按合法替代或缺口处理，不伪称 helper 已执行；用确定性助手处理明确
  输入和格式，不能替 Agent 改结论或 owner。独立核验首次完整，修正只回查直接影响。
- [x] 每项改动增加正常及负向 case：必要规则变化须回读、同名不等价材料不能误用、工具失败
  不报成功、未完成合理替代不标不可得、核验缺失不交付。原工作包文件及更新时机保留。
- [x] 为 07 固定顺序／并行两侧同一问题契约、最小上下文与质量要求，至少三个独立域真实重叠；
  数值沿用 02 的获批目标。模型、工具、授权或网络差异逐项说明，不以版本指纹判断等价。
- [x] 将已实现点、预期减少动作、短样本结果及完整性能尚缺证据回写 impact-map；此时不写
  “已提速 20%”。若需要新的完整对照，合入 07 最小补验清单并单独取得授权。

定向命令：

```bash
.venv/bin/python -m pytest -q tests/product/skill/test_phase5_parallel_contract.py tests/product/skill/test_work_package_contract.py tests/product/skill/test_phase5_execution_contract.py
```

预期：核心依赖、目录和引用规则通过，冲突／越权结构反例失败；P01–P06 的行为结果另行验证。
修改 canonical 后按路线图更新 manifest 并跑工程门禁。
  （04.3 完成，2026-09-12：提交 611e1cb——impact-map §5 回写（六行审计全部由 03+04 覆盖、
  四栏＋07 对照契约十项＋数值待定＋三独立域重叠＋不宣称未验证收益）、静态断言 8 项
  （20 条正常/负向 case 引用逐一验证真实存在）＋任务评审通过；docs 追加段如实声明
  并行收益证明留 07）
## 6. 停止、回退与完成

跨任务污染、依赖未就绪综合、授权再审遗漏或取消后采用均阻断对应能力；保留失败并修最小
范围。无原生并行或重叠不可观测时保留能力缺口，不以独立审稿或多股票同时运行替代。
回退本次受影响 Prompt／助手及合同，不删除失败研究或关闭全部正常研究。阶段完成须有
P01–P06 和必要反例通过；正式并行至少 20% 的端到端中位收益、调用不超基线、token 上限及
全流程总目标在 07 分别验收，二者不能互相抵扣。
  （阶段 04 执行完成，2026-09-12：04.1–04.3 提交 d7f49b0／836af1f／611e1cb；行为证据
  P01–P06 全部通过（.hetu/validation/phase-5/20260912-stage04-parallel/p-samples-summary.md）；
  §6 阻断项检查：跨任务污染、依赖未就绪综合、授权再审遗漏、取消后采用——P03–P06 均按合同
  拒绝/隔离/门控，未发生；无原生并行宿主组合未验证，登记为能力缺口不声明支持。20% 收益
  等性能验收按本条留 07 配对完整研究分别验收，本阶段未宣称提速。终审结论见最终评审记录。）


## 7. 用户批准的验收安排（2026-09-12）

用户接受本阶段合同实现与决策验证作为开发级收尾。P01 的派发计划、P04 的重派与汇合处置、
P06 的越权拒绝及正常任务继续，现有记录证明决策逻辑，不证明对应动作已经实际执行。
本章原完成勾选与样本 PASS 按各自实际覆盖范围解释，不作为原生并行能力认证。

实际执行缺项移至 [阶段 07](2026-09-08-07-host-and-final-acceptance.md) 的一条受控短链路，
只补缺项，不重跑全部 P 样本，不需要股票分析。20% 收益与其他完整性能要求仍按 07 原计划
单独验收。未验证能力不得声明正式支持，本批准不启动 07 或任何股票分析。
